#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    // char x,y; cin>>x>>y;
    // if(x==y) cout<<"YES";
    // else cout<<"NO";
  //ascii
  //a-97
  //A-65
  //0-48 (char form 0)
  char x = 'a';
  cout<<(int)x;
  int y = 98;
  cout<<(char)y;  
    return 0;
}